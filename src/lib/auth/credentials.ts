/**
 * Re-exports shared auth constants. Credential persistence lives on the server.
 */
export {
  DEMO_EMAIL,
  DEFAULT_PASSWORD,
  MIN_PASSWORD_LENGTH,
  isDemoEmail,
  SESSION_COOKIE,
} from "./constants";
